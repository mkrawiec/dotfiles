# Configuration specific to szwalnia-thinkpad.linux hostname

# Virtualfish support
eval (python3 -m virtualfish)

set -U fish_user_paths ~/opt/android-sdk/platform-tools $fish_user_paths
set -x ANDROID_HOME '/home/maciek/opt/android-sdk'
